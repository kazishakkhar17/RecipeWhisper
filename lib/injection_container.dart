import 'package:get_it/get_it.dart';

final sl = GetIt.instance;

Future<void> init() async {
  // Register services or repos here later.
  // Example:
  // sl.registerLazySingleton<AuthRepository>(() => AuthRepositoryImpl());
}
